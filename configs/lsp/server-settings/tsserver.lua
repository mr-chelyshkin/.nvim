return {
    settings = {
        -- enables additional diagnostic messages in tsserver
        diagnostics = {
            enable = true,
        },
        -- configures automatic updating of imports when renaming files
        updateImportsOnFileMove = {
            enable = "always",
        },
    }
}
